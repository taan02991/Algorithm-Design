#include <iostream>
#include <map>

using namespace std;

double w, V[105] = {0}, W[105] = {0};
unordered_map<double, double> m;
int n;

int main()
{
    cin>>w>>n;
    for(int i = 1; i <= n; i++) scanf("%d", &V[i]);
    for(int i = 1; i <= n; i++) scanf("%d", &W[i]);
    s()
    return 0;
}
